let tupla:[string, number, boolean] = ['Alexis', 10, false];
let tupla2: [string[], number[], boolean];

tupla2 = [['Alexis, Ricardo'], [1020, 20], false]

/**tuples is like in python but here you need to defined the value */

console.log(tupla, tupla2)